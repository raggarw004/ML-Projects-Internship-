# Project 01 — House Price Prediction (Regression)

Predict California house prices from tabular features using:
- Baseline: Linear Regression
- Better model: RandomForestRegressor

## Dataset
This project uses scikit-learn's `fetch_california_housing` dataset.

## How to run

### 1) Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

## Note

One trained model file is excluded from this repository due to GitHub file size limits. Other model files are still included, and the missing one can be recreated by rerunning training locally.